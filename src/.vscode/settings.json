{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "words-test",
            "database": "${workspaceFolder:src}/words.db"
        }
    ]
}